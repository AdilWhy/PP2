import re

txt = input()

# t = re.findall('ab*', txt)

# t = re.findall('ab{2,3}', txt)

# t = re.findall('[a-z]+_[a-z]+', txt)

# t = re.findall('[A-Z][a-z]+', txt)

# t = re.findall('a.*b$', txt)

# t = re.sub('[ ,.]', ':', txt)

# t = re.sub(re.compile('(?:^|_)(.)'), lambda x: x.group(1).upper(), txt)

# t = re.findall('[A-Z][^A-Z]*', txt)

# t = re.sub('(?<!^)(?=[A-Z])', ' ', txt)

# t = re.sub(re.compile('(?<!^)(?=[A-Z])'), '_', txt).lower()

print(t)